apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend
  namespace: mandagsmiddag
spec:
  entryPoints:
    - public
  routes:
    - kind: Rule
      match: Host(`mandagsmiddag.no`)
      services:
        - name: mandagsmiddag-oauth2-proxy
          port: 80
