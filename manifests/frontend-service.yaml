apiVersion: v1
kind: Service
metadata:
  name: mandagsmiddag-frontend
  namespace: mandagsmiddag
  labels:
    app.kubernetes.io/name: mandagsmiddag-frontend
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app.kubernetes.io/name: mandagsmiddag-frontend
  type: ClusterIP
