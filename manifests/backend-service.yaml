apiVersion: v1
kind: Service
metadata:
  name: mandagsmiddag-backend
  namespace: mandagsmiddag
  labels:
    app.kubernetes.io/name: mandagsmiddag-backend
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: mandagsmiddag-backend
  type: ClusterIP
